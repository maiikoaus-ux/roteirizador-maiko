Arquivo: landing-unscoped.css

CSS da landing Lovable na versão global (antes do escopo #public-landing).
O app em produção usa apenas: assets/css/landing-scoped.css

Para regenerar o scoped a partir desta fonte, use o fluxo do repositório (script ou cópia manual), não edite landing-scoped.css à mão sem necessidade.
